import React from "react";

const Saved = props =>
        <div>
            <h1>Saved</h1>
            {props.title}
            {props.date}
            {props.url}
        </div>

export default Saved;
